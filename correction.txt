# Si Francko a nvm
nvm install 22
nvm use 22
node -v   # → v22.x.x

# Vérifier que c'est bien lu
cat .nvmrc   # → 22

# Réinstaller proprement
rm -rf node_modules
npm install

# Relancer
npm run dev:server