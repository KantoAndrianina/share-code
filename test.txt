{/* TEST TOAST — à supprimer après vérification */}
<div style={{ display: 'flex', gap: '10px', marginTop: '20px' }}>
  <button
    style={{ padding: '8px 14px', background: '#16a34a', color: 'white', border: 'none', borderRadius: '6px', cursor: 'pointer' }}
    onClick={() => toast.success('Film ajouté ✓')}
  >
    Test success
  </button>
  <button
    style={{ padding: '8px 14px', background: '#dc2626', color: 'white', border: 'none', borderRadius: '6px', cursor: 'pointer' }}
    onClick={() => toast.error('Connexion impossible')}
  >
    Test error
  </button>
  <button
    style={{ padding: '8px 14px', background: '#2563eb', color: 'white', border: 'none', borderRadius: '6px', cursor: 'pointer' }}
    onClick={() => toast.info('Information')}
  >
    Test info
  </button>
</div>